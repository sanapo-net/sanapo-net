# Спецификация: Модульный класс, BufferTcp

## Данные
файл:
юнит:
кодер:
статус: запланирован

## Тесты


## Принимаемые сообщения


## Отправляемые сообщения


## Общее тех-задание


Содержит модуль (объект) `=link(this.module_class)`. Работает в своем потоке `[[buffer_tcp.thread]]`.
Накапливает и структурирует информацию в ОЗУ в виде вложенного словаря: `dict[host_uid, dict[port_num, list[list[datetime, bool]]]]`. 
Внутри списка проверок конкретного порта хронологически последняя запись (последнее сканирование) всегда находится в самом конце списка.

## Приватные методы


## Особое
