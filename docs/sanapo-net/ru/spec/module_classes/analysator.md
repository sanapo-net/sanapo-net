# Спецификация: Модульный класс, Analysator

## Данные
файл:
юнит:
кодер:
статус: запланирован

## Тесты


## Принимаемые сообщения


## Отправляемые сообщения


## Общее тех-задание


Содержит модуль (объект) `=link(this.module_class)`. 
Для эффективной работы без лагов запрашивает оперативные 1, 3 и 10-минутные данные напрямую из быстрой "in-memory" матрицы юнита [[buffer_icmp.unit]]. Более старые исторические срезы (за час, сутки, неделю) получает в виде оптимизированных пакетов от менеджера баз данных [[db_icmp.unit]] через сообщения-события фреймворка [[sanapo.fw]].

## Приватные методы


## Особое
